# textures/

Empty on purpose. The report-sheet texture and the contact shadow are drawn
at runtime on a canvas (`src/three/reportSheets.js`, `src/three/stage.js`).
Put compressed (WebP/KTX2, ≤ 2K) textures here only if a future GLB needs them.
