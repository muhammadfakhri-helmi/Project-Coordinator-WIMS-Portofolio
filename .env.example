# Optional. Base path for the production build.
# "./" (default) works from any folder or a GitHub Pages project subpath.
# Use "/" for a custom domain root, or "/<repository-name>/" for an absolute Pages path.
BASE_PATH=./
